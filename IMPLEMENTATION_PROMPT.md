You are building a production-quality course website in Docusaurus + React.

Your task is to implement a high-fidelity, maintainable, scalable course site for the course:

AI Systems Engineering
Subtitle: Design of LLM & Multimodal AI Applications

You will be given:
1. The complete course curriculum and module structure as content context
2. An attached landing-page image that should be treated as the PRIMARY visual reference
3. An earlier HTML design/mockup that should be treated as SECONDARY structural/style inspiration, not the final visual source of truth

PRIMARY GOAL
Replicate the attached landing page with high fidelity in Docusaurus + React while building the project in a way that is highly maintainable, modular, scalable, and easy to extend over time.

GENERAL EXPECTATIONS
- Build a real Docusaurus project, not a one-off static page
- Use React components for reusable UI
- Keep UI components decoupled from content/data
- Make it easy to add new modules, lectures, sections, labs, projects, and resources without editing many files
- Favor a content-driven architecture
- Use clean, readable, production-grade code
- Prioritize maintainability and extensibility over shortcuts
- Avoid tightly coupling visual components to hardcoded course content
- Design for future growth across many pages and many modules

VISUAL TARGET
Use the attached landing-page image as the main design source of truth.

The landing page should match the image as closely as practical in:
- layout
- spacing
- typography hierarchy
- card structure
- border radius
- shadows
- visual density
- section ordering
- navbar/footer style
- overall polish
- dark blue primary theme color
- clean text-led hero section
- modern educational / docs-platform feel

Important visual direction:
- Light theme by default
- Dark blue is the primary brand/accent color
- Background should be soft and minimal, with subtle abstract shapes or gradient texture if needed
- Avoid teal/green as the main theme color
- Preserve a premium academic-tech aesthetic
- Keep the page uncluttered and breathable
- The final landing page should NOT include the old AI architecture card on the hero right side
- Hero should be text-led and balanced with subtle decorative background instead of a large hero dashboard panel

INFORMATION ARCHITECTURE
The site should support:
- landing page
- curriculum overview
- module pages
- lecture pages
- lab pages
- project pages
- resource pages

It must be designed so the curriculum can grow substantially over time.

TECH STACK
- Docusaurus
- React
- TypeScript preferred
- MDX for course content
- CSS Modules and/or a clean theme token system
- Avoid unnecessary dependencies
- Only add libraries that clearly improve maintainability or user experience

CONTENT ARCHITECTURE
Use a content-first architecture.

Course content must be represented separately from UI, ideally through:
- MDX files with frontmatter
- JSON/TS data objects where appropriate
- reusable content schemas/interfaces
- autogenerated or low-maintenance sidebars where possible

Do NOT hardcode the full course content inside JSX component files.

The course curriculum the user provided should be used as the content basis for:
- landing page module cards
- curriculum page
- module index pages
- internal doc structure

SCALABILITY REQUIREMENTS
The project must be designed so that adding any of the following is low friction:
- a new module
- a new lesson
- a new lab
- a new project
- a new resource collection
- a new section on the homepage
- a second course in the future

This means:
- reusable cards
- reusable page templates
- reusable section wrappers
- centralized tokens/theme values
- structured content collections
- minimal repetition

FILE / FOLDER ORGANIZATION
Follow React and Docusaurus best practices with a clean modular structure similar to:

src/
  components/
    layout/
    landing/
    curriculum/
    cards/
    ui/
    icons/
  data/
    site/
    curriculum/
  theme/
  css/
    tokens.css
    global.css
    utilities.css
pages/
docs/
  intro.mdx
  curriculum/
  modules/
    module-01/
      index.mdx
      lecture-01.mdx
      lecture-02.mdx
      lab-01.mdx
  resources/
static/
  img/
  icons/
  illustrations/

Expectations:
- each component should have a single responsibility
- avoid giant component files
- separate layout components from domain/content components
- separate generic UI primitives from course-specific compositions

COMPONENT DESIGN REQUIREMENTS
Create reusable components for at least:
- Navbar
- Footer
- Hero section
- Section container
- Feature card
- Module card
- CTA buttons
- Badge/pill
- Content grid
- Curriculum overview block
- Page header for module/lecture pages
- Resource/action buttons
- Optional callout/info blocks for docs pages

Components should:
- accept props cleanly
- avoid hardcoded course-specific values unless intentionally course-specific
- be reusable across landing page and docs pages
- be composable rather than monolithic

LANDING PAGE REQUIREMENTS
Implement a high-fidelity landing page matching the attached image.

Expected sections:
1. Top navbar
   - brand/logo
   - key nav links
   - primary CTA
2. Hero section
   - badge
   - main title
   - subtitle
   - supporting paragraph
   - two CTA buttons
   - subtle decorative background, not a large right-side architecture panel
3. What You’ll Learn section
   - four feature cards
4. Course Modules section
   - grid of module cards
5. Footer
   - organized links
   - email/newsletter block if present in design
   - clean lower meta links area

Important:
- Keep the landing page lean
- Match spacing and hierarchy carefully
- Ensure responsive behavior across desktop/tablet/mobile
- Avoid awkward wrapping and overcrowding
- The hero must remain elegant without the removed architecture card

COURSE DATA REQUIREMENTS
The module cards and curriculum pages should be driven from structured data.
At minimum, define metadata for:
- course title
- subtitle
- description
- module number
- module title
- module summary
- module slug
- module icon
- optional lecture/lab/project counts
- optional tags

If appropriate, implement a small typed course-data layer so homepage cards and docs pages can pull from the same source.

DOCUSAURUS REQUIREMENTS
- Configure a proper Docusaurus site structure
- Use docs for long-form content
- Use MDX for content authoring flexibility
- Set up sidebars in a maintainable way
- Prefer autogenerated sidebars where sensible
- Ensure navbar/footer config is organized
- Support internal linking cleanly
- Ensure the project can scale to many docs pages

AUTHORING EXPERIENCE
Optimize for future content creation.

Add a structure that makes authoring easy:
- clear MDX templates or examples
- predictable folder naming
- frontmatter conventions
- reusable embedded components for notes, resources, learning objectives, prerequisites, etc.

The goal is that future contributors can add course content without touching core UI files.

THEMING / DESIGN SYSTEM
Create a small design system using tokens:
- primary dark blue
- supporting neutrals
- text colors
- border colors
- card shadows
- radius values
- spacing scale
- typography scale

Use CSS variables or equivalent token strategy.
Do not scatter magic numbers and colors across many files.

Typography should feel modern, polished, and readable.
Prefer clear hierarchy and consistent vertical rhythm.

RESPONSIVENESS
The site must behave well on:
- large desktop
- laptop
- tablet
- mobile

Requirements:
- navbar adapts cleanly
- hero text remains readable
- cards stack elegantly
- module grid becomes responsive
- footer remains organized
- no horizontal overflow
- spacing remains balanced

ACCESSIBILITY
Implement with good accessibility defaults:
- semantic HTML
- proper heading hierarchy
- accessible contrast
- keyboard-navigable interactive elements
- focus states
- alt text for images/illustrations
- aria labels where needed

PERFORMANCE
- Keep bundle size reasonable
- Avoid heavy animation libraries unless necessary
- Use lightweight visual effects
- Optimize images/assets
- Avoid unnecessary runtime complexity on the landing page

CODE QUALITY
- Use TypeScript if possible
- Strong prop typing
- Clean naming
- Small focused components
- No duplicated markup patterns when reusable abstractions are appropriate
- No dead code
- No placeholder lorem ipsum in final output
- Keep code easy to understand for future maintainers

DELIVERABLES
Implement:
1. Docusaurus project structure
2. Landing page matching the attached image with high fidelity
3. Maintainable theme/token system
4. Structured curriculum data layer
5. Sample docs/module pages generated from the provided curriculum
6. Reusable components for expansion
7. Clear README notes for future maintainers

Also provide:
- explanation of project structure
- explanation of where to add new modules/content
- explanation of how homepage content is sourced
- explanation of how theme customization works

IMPORTANT IMPLEMENTATION PRINCIPLES
- The attached landing-page image is the primary visual reference
- The supplied curriculum must drive the content structure
- The older HTML design is only a secondary reference for patterns and overall academic-docs direction
- High fidelity in visual implementation matters
- Long-term maintainability matters equally
- Prefer elegant abstraction over quick hacks
- Build the project as if it will be maintained and expanded for years

SUCCESS CRITERIA
A successful result should:
- look very close to the attached landing-page image
- feel like a polished Docusaurus + React course site
- be easy to extend with new course content
- have strong separation of concerns
- use reusable components
- avoid hardcoded page-specific mess
- provide a clean foundation for the full course website